{
  "installCommand": "pnpm install && cd api && npm install",
  "buildCommand": "NODE_ENV=production pnpm --filter buildfrom-video build",
  "outputDirectory": "artifacts/buildfrom-video/dist/public",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
