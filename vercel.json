{
  "installCommand": "cd artifacts/buildfrom-video && npm install && cd ../../api && npm install",
  "buildCommand": "cd artifacts/buildfrom-video && NODE_ENV=production npm run build",
  "outputDirectory": "artifacts/buildfrom-video/dist/public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}